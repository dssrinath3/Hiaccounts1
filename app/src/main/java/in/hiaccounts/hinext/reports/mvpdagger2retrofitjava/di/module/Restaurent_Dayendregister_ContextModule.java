package in.hiaccounts.hinext.reports.mvpdagger2retrofitjava.di.module;

import android.app.Activity;
import android.content.Context;

import dagger.Module;
import dagger.Provides;
import in.hiaccounts.hinext.reports.mvpdagger2retrofitjava.di.qualifier.ActivityContext;
import in.hiaccounts.hinext.reports.mvpdagger2retrofitjava.di.scopes.ActivityScope;
@Module
public class Restaurent_Dayendregister_ContextModule {
 /*   private Activity mainActivity;

    public Context context;


    public Restaurent_Dayendregister_ContextModule(Activity mainActivity) {
        this.mainActivity = mainActivity;
        context = mainActivity;
    }

    @Provides
    @ActivityScope
    public Activity providesMainActivity() {
        return mainActivity;
    }

    @Provides
    @ActivityScope
    @ActivityContext
    public Context provideContext() {
        return context;
    }*/
}
